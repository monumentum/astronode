// const uri = 'test.com';
        // const port = 1000;
        // const database = 'test'
        // const user = 'test_user';

        // it('should exec #getDriver correctly', () => {
        //     const driver = adapter.getDriver({driver: 'mongoose', driverConfig: {uri, port, database, user}});
        //     expect(driver).toHaveProperty('uri', `mongodb://${uri}:${port}/${database}`);
        //     expect(driver).
        // });
describe('src/adapter/driver/mongoose', () => {
    it('should start', () => {});
})